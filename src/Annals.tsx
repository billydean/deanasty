import { Typography } from '@mui/material';

function Annals() {
    return (
        <Typography>
            These are the annals recording major events.
        </Typography>
    )
};

export default Annals;